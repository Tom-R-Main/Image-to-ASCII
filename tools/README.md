# Tools

Tooling that would make the core library heavier belongs here.

Planned tools:

- font calibration,
- render-to-image quality comparison,
- fixture generation,
- optional decoder experiments.

The core package should not depend on font rasterizers, image decoders, or Python tooling.
