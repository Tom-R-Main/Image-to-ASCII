.zig-cache/
zig-out/
zig-pkg/

.DS_Store
*.swp
*.swo

# Local-only benchmark and quality corpora.
bench/corpus/
tools/out/
testdata/generated/

# Fonts used by `zig build glyphshot` are fetched locally, never committed
# (size + licensing). See README "Visual verification".
*.ttf
*.otf
*.ttc
